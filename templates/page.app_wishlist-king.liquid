{{ 'option_selection.js' | shopify_asset_url | script_tag }}

<h1 class="small--text-center">{{ page.title }}</h1>

<div>
  {{ page.content }}
  <!-- include 'wishlist-collection{%- if page.handle contains 'shared' -%}-shared{%- endif -%}' -->
</div>